{
  "name": "syncup",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "e2e"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,yellow \"npm run dev -w client\" \"npm run dev -w server\"",
    "build": "npm run build -w client && npm run build -w server",
    "test:unit": "npm run test -w client && npm run test -w server",
    "test:e2e": "npm run test -w e2e"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "docx": "^9.7.1"
  }
}
